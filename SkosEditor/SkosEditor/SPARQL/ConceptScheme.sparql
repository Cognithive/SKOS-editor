PREFIX skos: <http://www.w3.org/2004/02/skos/core#>

CONSTRUCT
{
    ?scheme
        a skos:ConceptScheme ;
        skos:prefLabel ?label .
}
WHERE
{
    VALUES ?scheme { @id }

    ?scheme
        a skos:ConceptScheme ;
        skos:prefLabel ?label .
}